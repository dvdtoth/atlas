// Public deployment configuration. These are analytics routing IDs, never secrets.
// Set both values to enable the visitor's opt-in control. No data is sent by default.
export const analytics = Object.freeze({ endpoint: '', website: '' });
