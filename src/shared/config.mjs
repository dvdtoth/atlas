// Public deployment configuration. These are analytics routing IDs, never secrets.
// Forks and local builds are unconfigured. Set your own values to opt in to analytics.
// The official Pages workflow injects its configuration into dist/ only.
export const analytics = Object.freeze({ endpoint: '', website: '' });
